# MACO Co-design Studio

Start from the workspace root: `bash chia-maco/gui/start.sh`.
Open http://127.0.0.1:8765.

Independent CGRA application: array explorer, Pareto frontier, compiler results,
recorded playback, and explicitly confirmed live CHIA mapper searches.
Entry point: `src/main.ts`; compiled frontend: `dist`; new jobs: `runtime`.
Build/setup instructions: `../../gui/README.md`.
