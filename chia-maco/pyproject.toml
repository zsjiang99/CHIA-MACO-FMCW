[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "chia-maco"
version = "0.1.0"
description = "CHIA loop for FMCW compiler/CGRA architecture co-design"
requires-python = ">=3.10"
dependencies = [
  "chialoops==1.0.1",
  "openai>=1.0.0,<4",
  "numpy>=1.26,<3",
]

[project.optional-dependencies]
test = ["pytest>=8"]

[project.scripts]
chia-maco = "chia_maco.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
